# Update options for a higchartProxy object

Update options for a higchartProxy object

## Usage

``` r
hcpxy_update(proxy, ...)
```

## Arguments

- proxy:

  A `higchartProxy` object.

- ...:

  Named options.
